# 翻译流程

[English](../pipeline.md)

文译以“先建立全书理解，再按章稳定翻译”为默认策略。可以通过 `config.yaml` 关闭不需要的阶段，以降低成本或缩短时间。

```text
读取输入
-> 解析章节、正文段落和 EPUB 目录
-> 识别源语言或使用指定语言
-> 预扫整书，生成章节梗概和全书概览
-> 分析样章，建立初始术语表和风格指南
-> 按章、按批翻译
-> 按累积证据窗口抽取和更新术语
-> 可选润色与标点规范化
-> 使用最终术语库执行全书最终审校
-> 可选全书一致性 QA
-> 生成报告
-> 回填并组装指定格式的成品
```

## 全书理解与上下文

预扫会生成逐章梗概和全书概览。翻译每个批次时，提示词会按稳定信息优先的顺序提供角色风格、全书概览、章节梗概、相关术语表、最近译文和待译原文。

这使早期章节可以参考后续剧情，同时让同章相邻批次在代词、称谓、语气和跨段句意上保持衔接。

## 术语库

样章分析会建立初始术语库；翻译过程中先把已经完成的原译文累积成证据窗口，再抽取并更新人物、地名、组织、术语、招式和称谓。一个较大的术语窗口可以覆盖多个较小的翻译批次，既减少重复模型调用，也让抽取器获得更完整的上下文。调小 `pipeline.glossary_window_chars` 可更快把新词反馈给同章后续批次，调大则进一步降低调用成本。

每个模型候选都必须有双侧证据：原文写法必须出现在原文窗口中，中文译法也必须出现在对应译文中；未在原文窗口出现的候选别名会被丢弃。发送给抽取器的旧术语同样只保留本窗口出现的相关项。已完成窗口按实际原译文摘要建立检查点，因此内容未变的续跑不会调用模型；译文修改后也只会使受影响的窗口失效。

后续翻译批次默认只注入当前章节匹配到的词条，避免无关术语占用上下文。

术语表用于约束后续翻译和最终审校，但它不能自动保证已经完成的历史译文全部回写一致。可通过 `glossary list` 和 `glossary conflicts` 检查条目与冲突，再结合审校、QA、报告或人工调整。

## 质量控制

- **段数对齐**：模型必须返回与输入等长的 JSON 数组；失败会重试，仍失败时逐段兜底。
- **润色**：不改变原意和段落数量的前提下提升中文流畅度。
- **标点规范化**：统一为简体中文大陆常用全角标点。
- **最终审校**：全部章节翻译完成后才开始，因此每章都从最终术语库提取与本章相关的术语快照，而不是使用某一章翻译结束时的中间状态。章节按连续文本块并行检查漏译、增译、误译、术语和人称，结果按全书原顺序合并。只有开启 `autofix_severe` 才会重译严重问题。
- **全书一致性 QA**：在收尾阶段检查术语、人称、语气和标点的一致性，默认只报告问题。

最终审校默认关闭。设置 `pipeline.review: true` 后，一键流程会在翻译与 QA 之间
插入最终审校；也可以将它作为独立、可断点续跑的阶段使用：

```bash
uv run trans-novel review book.epub
uv run trans-novel review book.epub --force
uv run trans-novel review book.epub --fix     # --no-fix 覆盖为不自动修复
```

即使关闭 `pipeline.review`，显式调用上述命令仍会执行审校。`--force` 会重新
检查当前译文已完成审校的章节；`--fix` 和 `--no-fix` 会在当次运行中覆盖
`pipeline.autofix_severe`。

## 断点续跑

每个完成批次都会立即写入状态目录。再次运行 `translate` 会跳过已有译文，仅补齐未完成部分；`assemble` 可以直接从状态目录重新导出成品。
